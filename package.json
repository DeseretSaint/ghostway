{
  "name": "ghostway",
  "version": "0.1.0",
  "private": true,
  "description": "Ghostway — open-source navigation that routes you clear of ALPR / Flock surveillance cameras by default.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host --port 4173",
    "serve": "node scripts/serve-https.mjs",
    "fetch-cameras": "node scripts/fetch-cameras.mjs"
  },
  "devDependencies": {
    "@mapbox/vector-tile": "^3.0.0",
    "pbf": "^5.1.2",
    "puppeteer-core": "^23.11.1",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "maplibre-gl": "^4.7.1"
  },
  "license": "GPL-3.0-or-later"
}
