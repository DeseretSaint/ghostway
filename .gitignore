node_modules/
dist/
certs/

# Engine build inputs — regenerable (Geofabrik extract + DeFlock snapshot).
engine/data/*.pbf
engine/data/*.osm.pbf
engine/data/*.geojson

# Ship only the gzipped national WZDx snapshot (raw is a build artifact).
public/data/wzdx-national.json

# Ship only the gzipped graph.
public/graph/wasatch-graph.bin

# Test artifacts
engine-e2e.png
shot-route.png
shot-donate.png

# Loop lock files (ephemeral, never commit)
.ghostway-*.lock

# test screenshots
follow-shot.png
nav-playback.png

ob-shot.png

banner-full.png
banner-compact.png

# UX polish loop working screenshots (regenerate via scripts/ux-shots.mjs)
ux-shots/
android/local.properties
android/.gradle/
android/build/
android/app/src/main/assets/
android/app/build/intermediates/
android/app/build/outputs/apk/debug/app-debug.apk
android/app/build/outputs/apk/debug/output-metadata.json
android/app/build/outputs/logs/
android/app/build/tmp/
